name: github pages

on:
  push:
    branches:
      - main
  workflow-dispatch:

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: "Dioxus Deploy"
        uses: DioxusLabs/deploy-action@0.1.2
